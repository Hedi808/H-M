# Generated by CloudMind
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: h-m-ingress
  namespace: cloudmind-staging
  labels:
    app.kubernetes.io/name: h-m
    app.kubernetes.io/managed-by: cloudmind
  annotations:
    kubernetes.io/description: "CloudMind generated ingress for h-m"
spec:
  rules:
    - host: h-m.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: h-m-backend
                port:
                  number: 8000
