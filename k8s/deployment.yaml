# Generated by CloudMind
apiVersion: apps/v1
kind: Deployment
metadata:
  name: h-m-backend
  namespace: cloudmind-staging
  labels:
    app.kubernetes.io/name: h-m
    app.kubernetes.io/component: backend
    app.kubernetes.io/managed-by: cloudmind
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: h-m
      app.kubernetes.io/component: backend
  template:
    metadata:
      labels:
        app.kubernetes.io/name: h-m
        app.kubernetes.io/component: backend
    spec:
      containers:
        - name: backend
          image: ghcr.io/your-github-username/h-m-backend:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: h-m-config
            - secretRef:
                name: h-m-secret
                optional: true
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

